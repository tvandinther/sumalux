{
  "name": "sumalux",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
		"client": "cd client && ng serve --open",
		"server": "cd server && npm run start:watch",
		"mongo": "cd mongo && docker start mongo_mongo_1",
		"redis": "docker start sumalux-redis",
    "start": "concurrently \"npm run client\" \"npm run server\" \"npm run redis\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
	},
	"devDependencies": {
		"concurrently": "^5.0.2"
	}
}
